let Post,posts;_e43‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_e43‍.w("../../modules/Post",[["default",["Post"],function(v){Post=v}]]);_e43‍.w("./index",[["default",["posts"],function(v){posts=v}]]);


// POST / api/ posts{
//   title : '제목',
//   body: '내용',
//   tags: ['태그1','태그2']
// }

       const write = async (ctx) => {
  const { title, body, tags } = ctx.request.body;
  const post = new Post({
    title,
    body,
    tags,
  });
  try {
    await post.save();
    ctx.body = post;
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const list = (ctx) => {};

       const read = (ctx) => {};

       const remove = (ctx) => {};

       const update = (ctx) => {};
