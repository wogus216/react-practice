let mongoose,Schema,bcrypt,jwt,func;_17e‍.x([["default",()=>_17e‍.o]]);_17e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);_17e‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_17e‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_17e‍.w("../../node_modules/joi/lib/index",[["func",["func"],function(v){func=v}]]);




const UserSchma = new Schema({
  username: String,
  hashedPassword: String,
});

UserSchma.methods.setPassword = async function (password) {
  const hash = await bcrypt.hash(password, 10);
  this.hashedPassword = hash;
};

UserSchma.methods.checkPassword = async function (password) {
  const result = await bcrypt.compare(password, this.hashedPassword);
  return result; //true / false
};

UserSchma.statics.findByUsername = function (username) {
  //this는 모델을 가리킨다. 여기서는 User
  return this.findOne({ username });
};

UserSchma.methods.serialize = function () {
  const data = this.toJSON();
  delete data.hashedPassword;
  return data;
};

UserSchma.methods.generateToken = function () {
  const token = jwt.sign(
    // 첫 번째 파라미터에는 토큰 안에 집어넣고 싶은 데이터를 넣습니다.
    {
      _id: this.id,
      username: this.username,
    },
    process.env.JWT_SECRET, // 두 번째 파라미터에는 JWT 암호를 넣습니다.
    {
      expiresIn: '7d', // 7일 동안 유효함
    }
  );
  return token;
};

const User = mongoose.model('User', UserSchma);
_17e‍.d(User);
